import { test } from '@playwright/test'

// Placeholder UI test; real route wiring required to render the DialogueCard in-app.
test.skip('DialogueCard shows action buttons when canDoId provided', async ({ page }) => {
  // This test will be implemented when a dedicated preview route exists.
})


